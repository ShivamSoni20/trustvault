[project]
name = "trustwork-marketplace"
description = "TrustWork - Decentralized Job Marketplace with Bidding & Auto-Release"
authors = ["TrustWork Team"]
telemetry = true
cache_dir = "./.cache"

[contracts.trustwork-marketplace-v7]
path = "contracts/trustwork-marketplace.clar"
epoch = "latest"
depends_on = ["usdcx"]

[contracts.usdcx]
path = "contracts/usdcx.clar"
epoch = "latest"
depends_on = []

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
trusted_sender = false
trusted_caller = false
callee_filter = false

[network.simnet]
name = "simnet"

[network.devnet]
name = "devnet"

[network.testnet]
name = "testnet"
node_rpc_address = "https://api.testnet.stacks.co:443"

[network.mainnet]
name = "mainnet"
node_rpc_address = "https://api.stacks.co:443"
